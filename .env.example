# Forja environment variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ── LLM API Keys (at least one required) ─────────────────────────
# Kimi (Moonshot AI) - primary provider for spec review and cross-model validation
KIMI_API_KEY=

# Anthropic (Claude) - used for research with web search and as LLM fallback
ANTHROPIC_API_KEY=

# OpenAI - tertiary fallback provider
OPENAI_API_KEY=

# ── Optional overrides ───────────────────────────────────────────
# Override default models (see config_loader.py for defaults)
# FORJA_MODELS_KIMI_MODEL=kimi-k2-0711-preview
# FORJA_MODELS_ANTHROPIC_MODEL=claude-sonnet-4-20250514
# FORJA_MODELS_OPENAI_MODEL=gpt-4o

# Override build timeouts (minutes)
# FORJA_BUILD_TIMEOUT_STALL_MINUTES=12
# FORJA_BUILD_TIMEOUT_ABSOLUTE_MINUTES=20
# FORJA_BUILD_MAX_CYCLES_PER_FEATURE=5
